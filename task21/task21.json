int[] pointA1 = {1, 2, 3};
int[] pointB1 = {4, 5, 6};
double Length(int[] pointA, int[] pointB)
    {
        int sq = 0;
        double total = 0;
        for (int i = 0; i < 2; i++){
        double sqr = Math.Pow(pointB1[i] - pointA1[i], 2);
        total = total + sq;
        }
        double root = Math.Sqrt(sq);
        Console.WriteLine($"Расстояние между отрезками в пространстве: {root}");
        return root;
    }
Length(pointA1, pointB1);